version: "1.1"
languages:
  lua:
    classes:
      ISMPPanel:
        extends: ISPanel
        staticFields:
          Type:
            defaultValue: '"ISMPPanel"'
        methods:
          - name: render
          - name: prerender
