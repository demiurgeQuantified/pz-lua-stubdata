version: "1.1"
languages:
  lua:
    classes:
      ISDropCorpseIntoContainer:
        extends: ISBaseTimedAction
        constructors:
          - parameters:
              - name: character
                type: IsoPlayer
              - name: targetContainer
                type: ItemContainer
        staticFields:
          Type:
            defaultValue: '"ISDropCorpseIntoContainer"'
        fields:
          targetContainer:
            type: ItemContainer
        methods:
          - name: isValid
            return:
              - type: boolean
          - name: start
          - name: perform
